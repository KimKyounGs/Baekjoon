#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <stack>
#include <cmath>
#include <cstring>
#include <deque>
#include <map>
#define INF 1e9
#define endl '\n'

using namespace std;

struct FireBall {
    int x, y, m, d, s;
};

int N,M,K;
FireBall graph[51][51];
int dx[] = {-1, -1, 0, 1, 1, 1, 0, -1};
int dy[] = {0, 1, 1, 1, 0, -1, -1, -1};

void Back() {
    
}

int main()
{   
    cin >> N >> M >> K;
    for (int i = 0; i < M; i ++) {
        int x, y, m, s, d;
    }

    
    return 0;
}