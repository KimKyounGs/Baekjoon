/*
아이디어 : 

*/

#include <iostream>
#include <vector>
#include <algorithm>
#include <queue>
#include <stack>
#include <cmath>
#include <cstring>

using namespace std;

int N;
int cnt = 0;
queue<int> q;

void BFS() {
    for (int i = 0; i < 10; i ++) {
        if (q.empty()) {
            q.push(i);
        }
        else {
            
        }
    }
}

int main() {
    ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
        
    cin >> N;
    
    BFS();
}
